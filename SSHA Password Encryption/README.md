# Java SSHA Password Encryption. 
Note: SSHA is salted SHA, we can not retrieve the password ( Well that is the point of it, right). 
With respect to LDAP we can bind and authorize the user/group but if password lost, it needs to be reset, Password retrieval is impossible. 
